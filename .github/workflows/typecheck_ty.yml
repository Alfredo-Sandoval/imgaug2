name: typecheck (ty)

on:
  pull_request:
  push:

jobs:
  ty:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install the project (runtime deps) so `ty` can resolve imports.
          # This avoids drift between `requirements.txt` and `pyproject.toml`.
          python -m pip install -e .
          python -m pip install ty

      - name: Type check
        run: |
          # Uses `pyproject.toml` for configuration (including `[tool.ty]` and overrides).
          ty check --output-format=github
