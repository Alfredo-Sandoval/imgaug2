numpy>=1.24,<3
scipy
Pillow
matplotlib
scikit-image>=0.14.2
opencv-python-headless<4.12
imageio
Shapely
# Optional extras:
# - `pip install "imgaug2[numba]"` for numba acceleration hooks (where supported)
# - `pip install "imgaug2[mlx]"` for Apple Silicon MLX acceleration
